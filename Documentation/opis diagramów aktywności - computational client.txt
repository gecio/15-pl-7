Diagram aktywnoœci - zlecenie rozwi¹zania problemu przez u¿ytkownika

Aplikacja kliencka nawi¹zuje po³¹czenie z serwerem g³ównym, u¿ywaj¹c adresu IP serwera zapisanego w pliku konfiguracyjnym. Wysy³a zapytanie o metadane - adresy IP serwerów backup'owych oraz nazwy klas problemów, które mog¹ byæ rozwi¹zane przez ten klaster obliczeniowy (takie, ¿e istnieje przynajmniej jeden task manager potrafi¹cy obs³u¿yæ dany typ problemu). Serwer przesy³a metadane do klienta. Nastêpnie u¿ytkownik wybiera spoœród dostêpnych nazw klas problemów typ zadania jakie ma zostaæ rozwi¹zane i wprowadza do programu wszystkie potrzebne dane wejœciowe. Aplikacja kliencka wysy³a do serwera zlecenie rozwi¹zania problemu i podane przez u¿ytownika dane w formacie XML.
Serwer odbiera zlecenie przys³ane przez klienta i umieszcza problem w kolejce problemów danego typu oczekuj¹cych na rozwi¹zanie. Zadanie znajduje siê w kolejce, dopóki któryœ z task managerów, potrafi¹cych rozwi¹zaæ problem tej klasy, nie zakoñczy obliczeñ i nie bêdzie móg³ siê nim zaj¹æ.



Diagram aktywnoœci - odczytanie wyniku rozwi¹zanego problemu przez u¿ytkownika

Po otrzymaniu ostatecznego rozwi¹zania od task managera, serwer umieszcza problem na liœcie ukoñczonych, ale jeszcze nieodczytanych rozwi¹zañ. Nastênie u¿ytkownik wybiera z listy problem, którego rozwi¹zanie chce zobaczyæ. Aplikacja kliencka wysy³a do serwera ¿¹danie pobrania wskazanego wyniku. Serwer wysy³a rozwi¹zanie odpowiedniego problemu do klienta i usuwa je z listy ukoñczonych zadañ. Klient odbiera wyniki i wyœwietla je u¿ytkownikowi.